# CPA + CPA-Manager ????

?????????? Bash ?????????? Debian/Ubuntu VPS ??????????? CLIProxyAPI + CPA-Manager?

??????

- `cli-proxy-api`????? `eceasy/cli-proxy-api:latest`
- `cpa-manager`????? `seakee/cpa-manager:latest`

???????`/opt/cliproxy-cpam`

> ??????????? nginx ?? `cpa-management-center`????????????????/??????????

## ????

- ???? / ?? CPA + CPA-Manager
- ?????????
- ??????????
- ???????????
- ????
- ????????????? CPA-Manager ??
- ?? API_KEY?MGT_KEY ?????
- ?? Codex OAuth ??????
- ?????????????
- ???? Docker / Docker Compose???????? Docker
- ???? UFW ?????????

## ????

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/y08lin4/cpa-cpam-manager-sh/main/cpa-cpam-manager.sh)
```

## ????

```bash
chmod +x cpa-cpam-manager.sh
./cpa-cpam-manager.sh
```

??????????

```bash
bash cpa-cpam-manager.sh menu
bash cpa-cpam-manager.sh install
bash cpa-cpam-manager.sh upgrade
bash cpa-cpam-manager.sh start
bash cpa-cpam-manager.sh stop
bash cpa-cpam-manager.sh restart
bash cpa-cpam-manager.sh status
bash cpa-cpam-manager.sh logs
bash cpa-cpam-manager.sh backup
bash cpa-cpam-manager.sh keys
bash cpa-cpam-manager.sh uninstall
bash cpa-cpam-manager.sh help
```

## ? key ??

```bash
API_KEY='sk-cpa-xxx' MGT_KEY='mgt-cpa-xxx' bash cpa-cpam-manager.sh install
```

?????????????

```bash
INSTALL_DIR='/opt/cliproxy-cpam' CPA_HOST_PORT='8317' CPAM_HOST_PORT='18317' bash cpa-cpam-manager.sh install
```

????? `API_KEY` ? `MGT_KEY`?????????

- `API_KEY`?`sk-cpa-` + ?? 48 ? hex
- `MGT_KEY`?`mgt-cpa-` + ?? 48 ? hex

???????

```text
/opt/cliproxy-cpam/.secrets.txt
```

?????????????

## ????

```text
CPA API: http://???IP:8317/v1
CPA ????: http://???IP:8317/management.html
CPA-Manager: http://???IP:18317/management.html
```

CPA-Manager ?? Setup?

```text
CPA ??: http://cli-proxy-api:8317
Management Key: ?????? MGT_KEY
```

???CPA ??????? `config.yaml` ?? `remote-management.secret-key` ???? bcrypt hash??? `.secrets.txt` ?????? bcrypt hash ????? Management Key?

## ??

```bash
bash cpa-cpam-manager.sh upgrade
```

??????????

```text
/opt/cliproxy-cpam/backups/pre-upgrade-YYYY-MM-DD-HHMMSS.tar.gz
```

## ??

```bash
bash cpa-cpam-manager.sh backup
```

?????????

```text
/opt/cliproxy-cpam/backups/cpa-cpam-backup-YYYY-MM-DD-HHMMSS.tar.gz
```

## ??

```bash
bash cpa-cpam-manager.sh logs
```

??????

- `cli-proxy-api`
- `cpa-manager`
- ?????? 120 ???

## ??

```bash
bash cpa-cpam-manager.sh uninstall
```

???????????????

- ??????? `docker compose down`????????
- ???????? `docker compose down` ???????

## Codex OAuth ??????

????? 10 ?????????

```bash
cd /opt/cliproxy-cpam
docker compose exec cli-proxy-api /CLIProxyAPI/CLIProxyAPI -no-browser --codex-login
```

??????????????? `ssh -L` ????????????????????

## ????

```text
???? .secrets.txt
???? API_KEY / MGT_KEY
??? key ?? issue?TG????
?????????? 8317/18317
```

?? `.gitignore` ????

- `.secrets.txt`
- `auths/`
- `logs/`
- `cpa-manager-data/`
- `backups/`

## ?????

????? `ufw` ????????????

```text
8317/tcp
18317/tcp
8085/tcp
1455/tcp
54545/tcp
51121/tcp
11451/tcp
```

?? `ufw` ???????????????? SSH???????????? SSH?CPA?CPA-Manager ??????? UFW?
