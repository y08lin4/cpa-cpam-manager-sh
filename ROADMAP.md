# CLIProxyAPI 与 CPA Manager Plus 功能路线图

## 1. 文档目标

本文面向本仓库后续维护，规划围绕两个核心上游项目可以继续增加的安装、运维、安全和自动化能力：

- **CLIProxyAPI**：负责模型代理、认证、路由和调用日志。
- **CPA Manager Plus**：负责可视化管理、用量查看和管理操作。

本仓库仍定位为轻量级 Bash 运维工具，不复制上游业务代码。优先通过稳定接口、容器能力和配置文件完成集成；涉及上游内部数据结构的功能必须先确认兼容性。

## 2. 现有能力基线

当前脚本已经具备：

- 新服务器一键部署 CLIProxyAPI 与 CPA Manager Plus。
- 旧 CPA-Manager 到 CPA Manager Plus 的统一迁移评估、正式迁移和回滚。
- 菜单、状态、升级与迁移共用的容器、镜像、端口和安装类型采集器。
- 带统一 v2 元数据的人工快照、系统保护点、迁移保护点和恢复能力。
- 计划任务中心：systemd 定时版本检查和自动快照、只针对 scheduled 快照的滚动保留和指定快照删除。
- Compose、配置、权限、SQLite、挂载和磁盘空间只读体检。
- 当前版本与目标版本对比、升级确认和无变化退出。
- 最近 24 小时消费行为与管理行为独立审计、各自成功/失败 IP 排名、内网统计和公网 IP 归属查询。
- ShellCheck、Bash 语法检查及关键流程模拟测试。

后续新增功能应复用这些基础能力，避免形成第二套安装目录、状态检测或快照格式。

## 3. 优先级原则

| 优先级 | 判断标准 | 建议周期 |
| --- | --- | --- |
| P0 | 能显著降低升级、数据丢失或配置错误风险 | 下一至两个版本 |
| P1 | 提升日常运维效率、可观测性和安全性 | 两至四个版本 |
| P2 | 面向多实例、规模化或高级自动化 | 基础能力稳定后 |

每项功能至少满足：操作前展示影响范围、危险操作默认不确认、失败时保留现场、输出可读且支持非交互自动化。

## 4. CLIProxyAPI 建议功能

### 4.1 P0：配置体检与差异展示

基础配置体检已经实现。后续在升级或重启前自动调用，并补充严格 YAML 解析、认证目录可写性和脱敏配置差异。

验收标准：

- 无效 YAML、重复端口和不可写目录会阻断操作。
- 密钥在差异中只显示前后少量字符。
- 预检不修改原配置，失败信息能定位到字段或路径。

### 4.2 P0：版本通道与固定版本

支持 `stable`、`latest` 和显式镜像标签三种升级策略，记录上一次成功运行的镜像摘要。生产环境默认推荐固定版本，不只依赖浮动的 `latest`。

验收标准：

- 升级页同时显示当前标签、镜像摘要和目标摘要。
- 能一键回滚到最近一次成功镜像。
- 镜像标签不变但摘要变化时明确提示供应链变化。

### 4.3 P1：认证与模型可用性巡检

在不输出密钥内容的前提下，统计认证文件数量、最近更新时间和异常文件；对已配置模型执行可选的轻量探测，区分网络失败、鉴权失败、限流和上游不可用。

验收标准：

- 探测默认关闭，执行前说明可能产生真实调用。
- 单个模型失败不会中断全部巡检。
- 输出按模型或上游分组，不直接打印请求正文和密钥。

### 4.4 P1：调用质量与容量摘要

从现有日志生成成功率、失败率、状态码分布、延迟分位数和高频模型摘要，支持最近 1 小时、24 小时和 7 天范围。

验收标准：

- 不依赖外部数据库也能生成基础摘要。
- 大日志采用流式处理，不把全部内容加载到内存。
- 日志格式无法识别时明确显示缺失项，不伪造零值。

### 4.5 P2：多实例配置档案

允许维护多个相互隔离的实例档案，例如生产、测试和不同客户环境，每个档案拥有独立 Compose 项目名、目录、端口和快照路径。

验收标准：

- 实例之间不能共享密钥文件或写入同一数据目录。
- 所有命令先显示当前实例名称。
- 删除实例时必须逐项确认容器、目录和快照保留策略。

## 5. CPA Manager Plus 建议功能

### 5.1 P0：数据完整性与恢复演练

快照完成后不仅检查压缩包可读性，还应在临时目录验证 Manager 数据文件、关键配置和权限。增加只读恢复演练，确认快照能够被当前版本识别。

验收标准：

- 恢复演练不覆盖生产目录。
- 快照清单包含文件数量、大小、校验值和创建版本。
- 数据结构不兼容时阻断正式恢复并给出回退建议。

### 5.2 P0：升级兼容性门禁

升级 Plus 前读取上游版本信息和数据格式提示，检查是否跨越需要迁移的版本。无法判断兼容性时，不执行静默升级。

验收标准：

- 明确显示“当前版本 → 目标版本”。
- 重大版本升级必须创建保护快照并二次确认。
- 健康检查或鉴权检查失败时自动恢复旧镜像和旧数据快照。

### 5.3 P1：管理员密钥轮换

提供管理员密钥安全轮换流程：生成新密钥、更新部署配置、重建 Plus、验证新密钥、确认后废弃旧密钥。

验收标准：

- 终端默认不显示完整新旧密钥。
- 验证失败时自动恢复旧密钥。
- 轮换操作写入不含密钥正文的审计记录。

### 5.4 P1：数据导出与脱敏诊断包

生成便于提交 Issue 的诊断包，包括版本、容器状态、健康检查、脱敏 Compose、配置结构和最近错误摘要，排除认证文件、完整 IP、密钥与业务请求正文。

验收标准：

- 创建前列出将要收集和排除的内容。
- 生成后再次扫描常见密钥格式。
- 用户明确确认后才允许复制或上传诊断包。

### 5.5 P2：只读运维视图

如果上游提供稳定接口，可增加只读摘要页或终端视图，集中显示服务健康、调用趋势、快照状态和待升级版本，不在本仓库重新实现完整管理后台。

## 6. 两个项目的联合能力

### 6.1 P0：统一灾难恢复

将 Compose、CLIProxyAPI 配置与认证、Plus 数据、镜像摘要和元数据写入同一快照清单，形成可验证的整套恢复点。

建议流程：

1. 预检磁盘空间和目标路径。
2. 短暂停止写入方并创建一致性快照。
3. 计算校验值并记录当前镜像摘要。
4. 自动恢复原运行状态。
5. 在隔离目录执行可选恢复演练。

### 6.2 已实现基础版：计划任务与保留策略

已经提供计划任务中心：systemd timer 定时检查两个服务镜像版本，并按计划创建不停机 `scheduled-*` 快照和按数量滚动保留。版本检查只记录结果，不自动升级；快照滚动只删除 `scheduled-*`。后续再扩展健康巡检、凭证检查和按天数保留。

### 6.3 P1：告警适配器

把连续健康失败、磁盘不足、快照失败、失败调用激增和异常来源 IP 汇总为统一事件，再按需接入 Webhook、邮件或其他通知渠道。

安全边界：通知正文不得包含完整密钥、请求正文或未经确认的大量访客 IP。

### 6.4 P1：安全巡检增强

- 为 IP-API 结果增加本地缓存和有效期，降低限流与隐私暴露。
- 分别统计成功榜、失败榜、鉴权失败榜和敏感路径扫描榜。
- 只生成 UFW、nftables 或反向代理封禁建议，不默认自动封禁。
- 对内网高频失败单独告警，用于发现错误代理配置或内部滥用。

### 6.5 P2：供应链校验

在上游提供条件允许时，校验镜像签名、摘要和软件物料清单；至少记录每次部署使用的不可变镜像摘要，便于审计和回滚。

## 7. 推荐实施阶段

### 阶段 A：可靠性优先

1. 配置预检与脱敏差异。
2. 固定版本、镜像摘要记录和一键回滚。
3. 快照清单、校验值和隔离恢复演练。
4. Plus 升级兼容性门禁。

完成标准：任何升级失败都能恢复到操作前的容器、镜像、配置和数据状态。

### 阶段 B：可观测与安全

1. 调用成功率、失败率、状态码和延迟摘要。
2. IP 归属缓存、敏感路径扫描榜和防火墙建议。
3. 脱敏诊断包。
4. 可选告警适配器。

完成标准：运维人员能在一次巡检中判断服务是否健康、失败集中在哪里、是否需要进一步调查。

### 阶段 C：自动化与规模化

1. 计划任务中心的健康巡检、凭证检查和报告任务。
2. 多实例配置档案。
3. 供应链校验。
4. 只读统一运维视图。

完成标准：自动化任务可重复执行、不会交叉污染实例，并能在失败时保留可审计记录。

## 8. 不建议直接加入的能力

- 自动封禁访问 IP 不纳入实现范围：可能误伤代理、共享出口和合法用户；未来最多生成带证据和撤销方式的只读建议。
- 自动上传完整日志：日志可能包含密钥、请求正文和访客信息。
- 未创建保护快照就执行跨大版本升级：无法保证 Plus 数据结构兼容。
- 在 Bash 中重写完整管理后台：会扩大维护面并与上游功能重复。
- 默认使用 `ASSUME_YES=1`：只应由明确了解风险的自动化环境启用。

## 9. 每项功能的统一验收清单

- [ ] 交互菜单仍为一级菜单，不增加多级选择路径。
- [ ] 操作前展示对象、版本、路径、影响范围和回退方式。
- [ ] 高风险操作默认选择否。
- [ ] 输出使用统一的成功、警告和失败图标。
- [ ] 不输出完整密钥、令牌或敏感请求正文。
- [ ] 支持接口不可用、网络超时、磁盘不足和容器不存在等失败场景。
- [ ] 增加对应模拟测试，并通过 `bash scripts/check.sh`。
- [ ] README、帮助文本和设计文档同步更新。
