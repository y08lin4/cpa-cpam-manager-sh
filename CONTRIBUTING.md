# 贡献指南

感谢参与 CPA Manager Plus 安装与运维脚本的改进。

## 开发原则

- 保持单脚本安装入口，避免给最终用户增加不必要依赖。
- 所有破坏性操作必须有明确确认和危险路径保护。
- 涉及 SQLite、密钥和迁移的数据操作必须先备份、再验证、可回滚。
- 新旧 Manager 兼容逻辑必须避免两个消费者同时连接同一个 CPA 用量队列。
- 用户可见文案和关键代码注释使用简体中文。
- 注释解释设计原因、风险边界和非显而易见的行为，不逐行复述代码。

## 本地检查

```bash
bash scripts/check.sh
```

基础检查包括 Bash 语法、Git 空白错误、终端界面模拟、一致性备份模拟、升级版本展示模拟、安全巡检 IP 提取模拟和 ShellCheck。ShellCheck 在本机已安装时执行，在 CI 中强制执行。

## 提交要求

- 提交前运行 `git status --short`，只暂存当前任务相关文件。
- Commit 信息使用简体中文。
- 标题概括用户可感知的变化。
- 正文说明背景、主要改动、验证方式和风险或兼容性边界。

## Pull Request 建议

PR 描述建议包含问题背景、实现方案、验证命令、兼容性影响和回滚方式。
