# 行为审计与无损清除设计稿

## 1. 设计目标

本设计用于把现有“成功调用 IP 排名”和“失败调用 IP 排名”改造成两套独立审计：消费行为审计和管理行为审计。每套审计内部再分别展示成功与失败，同时保证：

- 不删除 CLIProxyAPI 原始日志。
- 不修改 CPA Manager Plus SQLite、统计数据和配置。
- 不修改或删除 OAuth 凭证、API Key 和管理密钥。
- 用户执行“清除记录”后，下次默认只看到清除时间之后的新行为。
- 必要时仍能临时查看全部历史，避免真正丢失审计证据。

这里的“清除”定义为**推进审计显示基线**，不是删除源数据。

当前进度：两套只读审计、成功/失败榜单、管理操作明细、探测过滤和未分类计数已经实现；无损清除显示基线和查看全部历史仍属于后续阶段。

## 2. 两套审计结构

### 2.1 消费行为审计

消费行为审计只关注实际产生模型消费的接口，例如：

- `/v1/chat/completions`
- `/v1/completions`
- `/v1/responses`
- `/v1/messages`
- `/v1/images/*`
- `/v1/videos/*`
- `/v1beta/models/*`
- `/backend-api/codex/*`

建议输出：

| 字段 | 说明 |
| --- | --- |
| 时间 | 请求完成时间 |
| 结果 | 成功、客户端失败、服务端失败 |
| HTTP 状态 | 2xx、4xx、5xx |
| 来源 IP | 公网、内网或回环地址 |
| 方法与路径 | POST、GET 及脱敏后的接口路径 |
| 归属信息 | 公网 IP 的国家、地区、ASN 和运营商 |

消费行为审计内部固定分成两部分：

```text
消费成功 IP 排名
消费失败 IP 排名
```

两张榜单只统计模型消费端点，不让管理操作、管理页面访问和健康检查污染结果。

### 2.2 管理行为审计

管理行为审计关注可能改变服务状态、凭证或配置的接口，例如：

- `/v0/management/*`
- 凭证文件上传、删除、启用和禁用。
- 修改 `config.yaml` 对应的管理接口。
- 重载配置、更新认证、切换账号状态。
- CPA Manager Plus 对 CPA Management API 发起的管理请求。

管理行为审计同样固定分成成功和失败两部分：

```text
管理操作成功 IP 排名
管理操作失败 IP 排名
```

除 IP 排名外，还应突出“做了什么”，例如上传凭证、删除凭证、修改配置或启停账号。

建议事件类型：

| 事件 | 风险级别 | 示例 |
| --- | --- | --- |
| 只读查看 | 低 | 查看配置、查看凭证列表 |
| 新增或上传 | 中 | 上传 OAuth 凭证、导入认证文件 |
| 修改或启停 | 中 | 禁用账号、修改配置 |
| 删除 | 高 | 删除认证文件、清理配置 |
| 密钥与安全配置 | 高 | 修改管理密钥、远程管理设置 |
| 未授权尝试 | 高 | 401、403、连续错误密钥 |

建议输出：

```text
时间                  结果  风险  来源 IP         方法    管理行为
2026-07-12 10:30:15   成功  中    172.19.0.1     POST    上传认证文件
2026-07-12 10:31:02   失败  高    203.0.113.10   DELETE  删除认证文件
```

请求正文、完整密钥和凭证内容不得写入审计结果。

### 2.3 排除项：系统访问与探测

以下请求不形成第三套审计，只从消费行为和管理行为榜单中排除：

- `/health`
- `/management.html` 及静态资源。
- `/v1/models` 等能力探测。
- Docker 健康检查。
- 反向代理存活检查。

系统访问只记录“已过滤多少条”的诊断数字，避免高频健康检查占满列表。

### 2.4 无法分类的请求

无法识别的路径不形成第三套审计。界面只显示“未分类请求数量”和少量脱敏样例，便于后续补充消费或管理分类规则。

## 3. 一级菜单设计

继续遵守项目的一级菜单要求，不增加层层进入的目录。

建议直接增加以下入口：

```text
15) 消费行为审计
20) 管理行为审计
21) 清除审计显示记录
22) 查看全部历史记录
```

原“安全巡检 / 24h IP”可以改名为“消费行为审计”，管理行为审计使用新的一级入口。两个入口内部都分别展示成功榜和失败榜。

命令行建议：

```bash
bash cpa-cpam-manager.sh audit-consumption
bash cpa-cpam-manager.sh audit-management
bash cpa-cpam-manager.sh audit-clear
bash cpa-cpam-manager.sh audit-all-history
```

## 4. 无损清除机制

### 4.1 核心原理

执行“清除审计显示记录”时，不运行 `truncate`、`rm`、`docker logs --clear`，也不修改 SQLite。

脚本只在安装目录写入一个独立状态文件：

```text
/opt/cliproxy-cpam/.audit-state.json
```

示例：

```json
{
  "version": 1,
  "consumption_visible_since": "2026-07-12T10:30:00+08:00",
  "management_visible_since": "2026-07-12T10:30:00+08:00",
  "updated_at": "2026-07-12T10:30:00+08:00"
}
```

下一次审计读取相应分类的 `visible_since`，只展示该时间点之后的记录。

### 4.2 可以选择的清除范围

清除操作内部可以选择：

1. 只清除消费行为审计的显示记录。
2. 只清除管理行为审计的显示记录。
3. 两套审计全部推进到当前时间。

操作确认页必须明确显示：

- 当前显示基线。
- 新显示基线。
- 不会删除的源数据路径。
- 如何恢复查看历史。

### 4.3 数据源过滤方式

Docker 容器日志：

```bash
docker logs --since <visible_since> cli-proxy-api
```

文件日志：

- 读取 `logs/main.log` 和轮转日志。
- 解析 CLIProxyAPI 官方 `[YYYY-MM-DD HH:MM:SS]` 时间前缀。
- 只保留时间不早于 `visible_since` 的记录。
- 没有可解析时间的行不混入排行榜，单独报告无法判断的行数。

这种方式不依赖文件偏移量，因此日志轮转、压缩或容器重建后仍然有效。

### 4.4 查看历史和撤销清除

提供两种恢复方式：

- **临时查看全部历史**：本次忽略 `.audit-state.json`，不修改基线。
- **重置显示基线**：删除对应分类的时间值，之后恢复默认最近 24 小时视图。

状态文件损坏时，不应阻断审计。脚本应告警并回退到最近 24 小时，同时保留损坏文件供排查。

## 5. 状态文件安全

- 使用临时文件写入后原子替换，防止中途断电留下半份 JSON。
- 权限设置为 `0600`。
- 文件只保存时间戳和版本，不保存密钥、IP、请求正文或统计结果。
- `.audit-state.json` 不属于上游程序数据，CLIProxyAPI 和 Plus 不读取它。
- 普通快照可以包含该文件，以保留用户的查看习惯；恢复时允许选择是否恢复显示基线。

## 6. 统计规则

### 6.1 成功与失败

- 2xx：成功。
- 4xx：客户端或鉴权失败。
- 5xx：服务端或上游失败。
- 3xx：重定向，单独统计，不计入成功或失败。
- 明确失败关键词但没有状态码：进入“结果未知的失败线索”，不与 HTTP 失败混算。

### 6.2 IP 统计

- 公网、内网、链路本地和回环地址都保留。
- 只有公网 IP 允许在用户确认后发送给 IP-API Batch。
- 多层代理场景需要区分直连地址和可信代理转发地址。
- 未配置可信代理时，不直接信任任意客户端提供的 `X-Forwarded-For`。

### 6.3 两套审计的成功与失败展示

消费行为审计分别输出消费成功、消费失败 IP 排名；管理行为审计分别输出管理操作成功、管理操作失败 IP 排名。管理行为还应按“事件类型 + 方法 + 路径 + 结果”展示明细，用于发现凭证上传、删除、配置修改和未授权尝试。

## 7. 不影响原程序的边界

清除审计显示记录时，以下内容禁止修改：

```text
/opt/cliproxy-cpam/logs/
/opt/cliproxy-cpam/auths/
/opt/cliproxy-cpam/config.yaml
/opt/cliproxy-cpam/.secrets.txt
/opt/cliproxy-cpam/cpa-manager-data/
Docker 容器日志
```

禁止调用：

- `rm` 删除日志或数据库。
- `truncate` 截断日志。
- 清空 SQLite 表。
- 删除 Docker 容器来达到“清日志”效果。
- 改变上游日志保留配置。

## 8. 实施阶段

### 阶段一：两套只读审计

- 增加消费端点和管理端点两套分类规则。
- 增加系统探测排除规则和未分类诊断计数，但不创建第三套审计。
- 使用真实 CLIProxyAPI 官方日志样例补测试。
- 两套审计各自输出成功榜、失败榜和少量事件明细。
- 不修改现有日志和状态文件。

### 阶段二：无损清除

- 增加 `.audit-state.json`。
- 实现分类基线推进、全部历史临时查看和基线重置。
- 增加原子写入、权限和损坏恢复测试。

### 阶段三：管理行为语义化

- 将凭证上传、删除、禁用、配置修改等路径映射为中文事件名称。
- 增加高风险行为和连续未授权尝试提示。
- 只在日志信息足够时给出结论，无法确认时显示原始脱敏路径。

## 9. 验收测试

- [x] 消费行为审计分别显示消费成功和消费失败。
- [x] 管理行为审计分别显示管理操作成功和管理操作失败。
- [x] 模型消费不会进入管理行为审计。
- [x] 凭证上传、删除和配置修改不会进入消费排行榜。
- [x] 健康检查和静态资源不会污染两类核心审计。
- [ ] 清除后旧记录默认不可见，新记录可以正常出现。
- [ ] 临时查看全部历史时旧记录仍然存在。
- [ ] 清除操作前后源日志、SQLite、认证目录校验值不变。
- [ ] 状态文件损坏时回退到最近 24 小时，不删除任何源数据。
- [ ] 内网 IP 保留，公网 IP 只有确认后才进行第三方归属查询。
- [ ] 输出不包含完整密钥、Authorization 请求头或凭证正文。

## 10. 建议结论

建议先实施阶段一，确认消费端点与管理端点分类符合实际部署日志，并确认两套审计中的成功/失败统计准确；随后再实现无损清除。不要用真正删除日志的方式实现“清除记录”，因为这会破坏故障排查、入侵取证和历史统计。
